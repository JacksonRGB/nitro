### Ignored
- bound the honest-essential-edges wait with a per-test deadline so a stalled `TestEndToEnd_*` no longer burns the full 90m global timeout, and emit the unconfirmed edge IDs on failure for diagnostics

### Added
- Add Prometheus metrics across the filtering-report pipeline covering SQS delivery, poison-queue handling, external endpoint behavior, the RPC client, and filtering throughput.

### Changed
- Rename metric namespace `arb/txfiltering/report/*` to `arb/filtering-report/*`.
